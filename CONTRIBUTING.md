# Contributing

## Commit Messages

Commit messages must use this format:

```text
type(scope): summary
```

Examples:

```text
feat(instance): add status check output
fix(config): remove invalid instance profile
docs(contributing): document commit message format
```

Use a short lowercase `type`, a concise `scope` for the affected area, and an
imperative summary.
